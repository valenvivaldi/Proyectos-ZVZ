extends layout

block content
  script(src="/socket.io/socket.io.js")
  script.
    var socket = io.connect('/'); //var socket = io.connect('http://localhost:3000');
    function FuncJoin(){
      socket.emit("join");
    }
  
  
  
  
  .container
    h1 Unete a un juego creado! 
    p.lead 
    br
    
    form(name="join",role="form",action="/joingame",method="post")
      each juego in games
        if (username != juego.owner)
          .cajabotonjoin
            button(type="submit",name="idgame",value='#{juego._id}',class="botonjoin") #{juego.owner}
          
          
    a(href='/')
      button.btn.btn-primary(type="button") cancelar
    br
    
